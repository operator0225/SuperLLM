# SuperLLM — 500MB 이하 초소형 LLM 설계

목표: 모델 파일 500MB 이하로, 특정 능력 축에서 Gemma 27~31B급 성능에 근접하는 모델.

## 1. 전제와 현실

500MB에 담을 수 있는 파라미터 수는 정밀도에 따라 결정된다:

| 정밀도 | 파라미터 수 |
|---|---|
| FP16 | ~250M |
| INT8 | ~500M |
| INT4 | ~1B |
| 삼진 1.58bit (BitNet) | ~2.5B |

Gemma 27B는 FP16 기준 ~54GB, 4비트로도 ~16GB다. 가중치의 대부분은 암기된
세계 지식이며 정보이론적으로 무손실 60배 압축은 불가능하다. 따라서 전략은
"압축"이 아니라 **지식을 가중치 밖으로 빼고, 저장 대비 연산을 극대화**하는 것.

### 디퓨전(이미지 모델 기술)을 채택하지 않는 이유

텍스트 디퓨전 LM(LLaDA, Mercury, Gemini Diffusion)은 생성 절차만 바꿀 뿐,
같은 품질에 필요한 파라미터 수는 AR 트랜스포머와 비슷하다. 디퓨전이 주는 것은
병렬 디코딩 속도이지 용량 절감이 아니다. 이미지 모델이 작은 이유는 이미지
신호의 중복성 때문이지 아키텍처의 마법이 아니다 (SD 1.5도 860M 파라미터).

## 2. 아키텍처: 4가지 축의 결합

### 축 1 — 삼진 가중치 (BitNet b1.58 방식)
- 가중치를 {-1, 0, +1}로 학습(QAT). 2B 파라미터가 ~400MB에 들어간다.
- 선례: Microsoft BitNet b1.58 2B4T (~400MB, CPU 실행 가능).
- 사후 양자화(PTQ)가 아닌 처음부터 삼진으로 학습해야 품질이 유지된다.

### 축 2 — 지식 외부화 (검색 메모리)
- 가중치는 추론/언어 엔진만 담당, 사실 지식은 디스크의 벡터+텍스트 인덱스에 저장.
- 선례: DeepMind RETRO — 검색 결합으로 25배 작은 모델이 GPT-3급 언어모델링 성능.
- "모델 500MB + 로컬 인덱스 N GB" 구조. 500MB 제약이 모델 파일에만 걸린다면
  이것이 성능 격차를 메우는 가장 큰 지렛대다.
- 학습 시점부터 retrieval-augmented로 학습해 모델이 검색 결과를 활용하도록 유도.

### 축 3 — 가중치 재사용 (recurrent depth)
- 동일 레이어 블록을 K회 반복 통과시켜 저장량 대비 유효 깊이를 K배로.
- 선례: MobileLLM의 블록 공유, Universal Transformer, latent recurrent-depth 모델.
- 난이도에 따라 반복 횟수를 가변으로 (쉬운 토큰은 얕게, 어려운 토큰은 깊게).

### 축 4 — 증류 + 테스트타임 컴퓨트
- 교사: Gemma 27B급 오픈 모델. 로짓 증류 + 추론 체인(CoT) 증류 병행.
- 작은 모델의 추론 격차는 긴 추론 체인 생성(test-time compute)으로 상당 부분
  회복 가능함이 증명되어 있다 (R1-distill 계열, s1 등).

## 3. 목표 스펙 (초안)

- 파라미터: ~2B (삼진) → 가중치 ~410MB + 임베딩/스케일 여유분 ≤ 90MB
- 토크나이저: 어휘 32k~64k, 임베딩 공유(tie), 임베딩은 INT8 허용
- 컨텍스트: 8k (검색 청크 삽입 포함)
- 어텐션: GQA + 슬라이딩 윈도우 혼합 (KV 캐시 절약)
- 반복 블록: 코어 8레이어 × 최대 4회 반복 = 유효 32레이어

## 4. 현실적인 성능 기대치

- 일반 지식 폭에서 31B급 동등은 불가능하다. 이것은 정직하게 인정하고 시작한다.
- 달성 가능한 목표: (a) 검색이 커버하는 도메인의 QA/요약, (b) 증류가 잘 먹는
  수학/코딩 추론 벤치마크 일부에서 4B~12B급 오픈 모델과 경쟁, 순간적으로
  27B급 점수에 근접하는 축을 만드는 것.

## 5. 단계별 계획

1. **P0 — 베이스라인**: 기존 소형 모델(Qwen3-1.7B 등)을 2bit 계열로 양자화해
   500MB 이하 베이스라인 수립, 평가 하네스 구축 (MMLU, GSM8K, HumanEval 축).
2. **P1 — 삼진 QAT**: 2B 삼진 모델을 증류 기반으로 학습 (교사 로짓 + CoT 데이터).
3. **P2 — 검색 결합**: 로컬 인덱스 파이프라인 + retrieval-aware 파인튜닝.
4. **P3 — 반복 깊이**: 블록 공유/가변 반복 도입, 저장량 재배분.
5. **P4 — 추론 증류**: long-CoT 데이터로 테스트타임 컴퓨트 능력 강화.

## 6. 리스크

- 삼진 QAT 사전학습은 수백 B 토큰 규모의 학습 연산이 필요 — 예산이 가장 큰 리스크.
  (완화: 기존 오픈 가중치를 초기화로 쓰는 continue-pretraining 경로 병행 검토)
- 검색 품질이 곧 체감 성능 — 인덱스 구축/갱신 파이프라인이 사실상 제품의 절반.
- 반복 깊이는 추론 지연을 늘림 — 500MB 제약과 별개로 속도 목표를 따로 정할 것.
