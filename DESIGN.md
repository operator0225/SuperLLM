# SuperLLM — 초소형 풋프린트 LLM 설계

목표 (개정): **RAM 상주 풋프린트 수백 MB** 수준을 유지하면서 Gemma 27~31B급
성능에 근접하는 모델. 두 가지 변형을 검토한다.

- **변형 A** — 모델 파일 자체가 500MB 이하 (원래 목표, §1~§6)
- **변형 B** — 1.58bit 가중치를 300MB 조각 × 30개(총 9GB)로 나누어
  UFS(플래시) ↔ RAM 간 스트리밍 (§7)

## 1. 전제와 현실

500MB에 담을 수 있는 파라미터 수는 정밀도에 따라 결정된다:

| 정밀도 | 파라미터 수 |
|---|---|
| FP16 | ~250M |
| INT8 | ~500M |
| INT4 | ~1B |
| 삼진 1.58bit (BitNet) | ~2.5B |

Gemma 27B는 FP16 기준 ~54GB, 4비트로도 ~16GB다. 가중치의 대부분은 암기된
세계 지식이며 정보이론적으로 무손실 60배 압축은 불가능하다. 따라서 전략은
"압축"이 아니라 **지식을 가중치 밖으로 빼고, 저장 대비 연산을 극대화**하는 것.

### 디퓨전(이미지 모델 기술)을 채택하지 않는 이유

텍스트 디퓨전 LM(LLaDA, Mercury, Gemini Diffusion)은 생성 절차만 바꿀 뿐,
같은 품질에 필요한 파라미터 수는 AR 트랜스포머와 비슷하다. 디퓨전이 주는 것은
병렬 디코딩 속도이지 용량 절감이 아니다. 이미지 모델이 작은 이유는 이미지
신호의 중복성 때문이지 아키텍처의 마법이 아니다 (SD 1.5도 860M 파라미터).

## 2. 아키텍처: 4가지 축의 결합

### 축 1 — 삼진 가중치 (BitNet b1.58 방식)
- 가중치를 {-1, 0, +1}로 학습(QAT). 2B 파라미터가 ~400MB에 들어간다.
- 선례: Microsoft BitNet b1.58 2B4T (~400MB, CPU 실행 가능).
- 사후 양자화(PTQ)가 아닌 처음부터 삼진으로 학습해야 품질이 유지된다.

### 축 2 — 지식 외부화 (검색 메모리)
- 가중치는 추론/언어 엔진만 담당, 사실 지식은 디스크의 벡터+텍스트 인덱스에 저장.
- 선례: DeepMind RETRO — 검색 결합으로 25배 작은 모델이 GPT-3급 언어모델링 성능.
- "모델 500MB + 로컬 인덱스 N GB" 구조. 500MB 제약이 모델 파일에만 걸린다면
  이것이 성능 격차를 메우는 가장 큰 지렛대다.
- 학습 시점부터 retrieval-augmented로 학습해 모델이 검색 결과를 활용하도록 유도.

### 축 3 — 가중치 재사용 (recurrent depth)
- 동일 레이어 블록을 K회 반복 통과시켜 저장량 대비 유효 깊이를 K배로.
- 선례: MobileLLM의 블록 공유, Universal Transformer, latent recurrent-depth 모델.
- 난이도에 따라 반복 횟수를 가변으로 (쉬운 토큰은 얕게, 어려운 토큰은 깊게).

### 축 4 — 증류 + 테스트타임 컴퓨트
- 교사: Gemma 27B급 오픈 모델. 로짓 증류 + 추론 체인(CoT) 증류 병행.
- 작은 모델의 추론 격차는 긴 추론 체인 생성(test-time compute)으로 상당 부분
  회복 가능함이 증명되어 있다 (R1-distill 계열, s1 등).

## 3. 목표 스펙 (초안)

- 파라미터: ~2B (삼진) → 가중치 ~410MB + 임베딩/스케일 여유분 ≤ 90MB
- 토크나이저: 어휘 32k~64k, 임베딩 공유(tie), 임베딩은 INT8 허용
- 컨텍스트: 8k (검색 청크 삽입 포함)
- 어텐션: GQA + 슬라이딩 윈도우 혼합 (KV 캐시 절약)
- 반복 블록: 코어 8레이어 × 최대 4회 반복 = 유효 32레이어

## 4. 현실적인 성능 기대치

- 일반 지식 폭에서 31B급 동등은 불가능하다. 이것은 정직하게 인정하고 시작한다.
- 달성 가능한 목표: (a) 검색이 커버하는 도메인의 QA/요약, (b) 증류가 잘 먹는
  수학/코딩 추론 벤치마크 일부에서 4B~12B급 오픈 모델과 경쟁, 순간적으로
  27B급 점수에 근접하는 축을 만드는 것.

## 5. 단계별 계획

1. **P0 — 베이스라인**: 기존 소형 모델(Qwen3-1.7B 등)을 2bit 계열로 양자화해
   500MB 이하 베이스라인 수립, 평가 하네스 구축 (MMLU, GSM8K, HumanEval 축).
2. **P1 — 삼진 QAT**: 2B 삼진 모델을 증류 기반으로 학습 (교사 로짓 + CoT 데이터).
3. **P2 — 검색 결합**: 로컬 인덱스 파이프라인 + retrieval-aware 파인튜닝.
4. **P3 — 반복 깊이**: 블록 공유/가변 반복 도입, 저장량 재배분.
5. **P4 — 추론 증류**: long-CoT 데이터로 테스트타임 컴퓨트 능력 강화.

> 구현 착수: `distill/` — 닫힌 API 교사(Gemini)로 LFM2.5-350M 을 sequence-level
> 증류하는 파이프라인. 자세한 근거와 실행법은 `distill/README.md` 참고.
> 핵심 제약: (1) q4_k_m 은 학습 불가 → bf16 로 학습 후 배포 시 변환,
> (2) Gemini 는 top-20 logprobs 만 노출 + 토크나이저 불일치 → 전체 logit KL 대신
> 교사 응답 SFT.
> 교사 경로 두 가지: (2a) 닫힌 API/오픈 교사로 sequence-level SFT(`train_distill.py`),
> (2b) 오픈 웨이트 교사(Gemma 4 E2B/E4B, 4-bit 가능)로 cross-tokenizer 로짓 증류
> — vocab 불일치(Gemma 262k vs LFM2 65k) 때문에 정석 KL 대신 ULD 손실 사용
> (`train_distill_logit.py`).

## 6. 리스크 (변형 A)

- 삼진 QAT 사전학습은 수백 B 토큰 규모의 학습 연산이 필요 — 예산이 가장 큰 리스크.
  (완화: 기존 오픈 가중치를 초기화로 쓰는 continue-pretraining 경로 병행 검토)
- 검색 품질이 곧 체감 성능 — 인덱스 구축/갱신 파이프라인이 사실상 제품의 절반.
- 반복 깊이는 추론 지연을 늘림 — 500MB 제약과 별개로 속도 목표를 따로 정할 것.

## 7. 변형 B — 가중치 스트리밍 아키텍처 (300MB × 30, UFS↔RAM)

### 7.1 개념

1.58bit 가중치 총 9GB ≈ **~45B 파라미터**. Gemma 31B보다 파라미터가 많으므로
품질 목표 자체가 현실적이 된다. 제약이 "모델 크기 500MB"에서
"RAM 상주량 수백 MB + 스토리지 9GB"로 재정의된다.

선례: Apple "LLM in a flash" (활성 희소성 + 윈도잉 + 프리페치로 RAM 대비
2배 크기 모델 실행), AirLLM (레이어 단위 순차 로딩), DeepSpeed NVMe offload,
llama.cpp mmap.

### 7.2 병목: 스토리지 대역폭

디코딩은 토큰마다 활성 가중치 전체를 한 번씩 읽는다. UFS 4.0 실효 순차 읽기
~3.5GB/s 기준:

| 구성 | 토큰당 읽기 | 속도 |
|---|---|---|
| 덴스 45B 전체 스트리밍 | 9GB | ~0.4 tok/s |
| + 추측 디코딩 (배치 8) | 9GB/8토큰 | ~2–3 tok/s |
| MoE (토큰당 1/16 활성) | ~560MB | ~6 tok/s |
| MoE + 핫 익스퍼트 RAM 캐시 | 200–300MB | 10+ tok/s |

- 프리필은 여러 토큰이 한 번의 가중치 읽기를 공유하므로 연산 바운드 — 문제없음.
- DRAM 대역폭(50–70GB/s)은 UFS의 15–30배이므로 RAM 상주분은 병목이 아님.
- NAND는 읽기로 마모되지 않으므로 수명 문제는 없음. 단, 지속 고대역 읽기의
  전력·발열(서멀 스로틀링)이 모바일에서 실질 리스크.

### 7.3 성립 조건: 덴스가 아니라 MoE

덴스 45B 스트리밍은 0.4 tok/s로 대화용 부적합. 따라서 변형 B는 **파인그레인드
MoE**를 전제로 한다:

1. **조각 단위 = 익스퍼트 단위**: 300MB 파일 하나 = 익스퍼트 묶음 하나로 정렬해
   순차 읽기 패턴을 유지 (UFS 랜덤 읽기는 순차보다 크게 느림).
2. **핫 익스퍼트 LRU 캐시**: RAM 예산(예: 3–4GB) 내에서 자주 활성화되는
   익스퍼트 상주. 어텐션/임베딩/라우터는 항상 상주.
3. **선행 라우팅 + 프리페치**: 라우팅 결정을 1레이어 앞서 내려 다음 조각을
   미리 읽기 시작 — 읽기와 연산을 중첩.
4. **추측 디코딩 병행**: 상주 소형 draft 모델(변형 A의 결과물 재활용 가능)로
   가중치 읽기 1회당 여러 토큰 검증.

### 7.4 변형 A와의 관계

두 변형은 배타적이지 않다. 변형 A의 500MB 모델을 변형 B의 draft 모델
겸 상주 코어로 쓰고, 9GB MoE를 "느리지만 똑똑한" 검증자/보강 계층으로 두는
계층 구성이 최종 형태 후보. 지식 외부화(§2 축 2)는 양쪽 모두에 유효하다.

### 7.5 리스크 (변형 B)

- MoE 45B(1.58bit) 사전학습 비용은 변형 A보다 훨씬 크다 — 오픈 MoE 가중치의
  삼진 QAT 변환(continue-pretraining) 경로를 우선 검토.
- 익스퍼트 활성 분포가 균등하면 캐시 적중률이 낮아져 대역폭 병목으로 회귀 —
  학습 시 라우팅 지역성(locality)을 유도하는 보조 손실 필요.
- 기기별 UFS 세대 차이(UFS 3.1은 ~2GB/s)로 성능 편차가 큼 — 최소 사양 정의 필요.

## 8. 온디바이스 가속 — GPU/NPU 매핑 (S25+ / Snapdragon)

> 정정(§10 참조): 실제 모바일 LLM 추론의 기본 가속 경로는 **GPU(Adreno, OpenCL/
> Vulkan)** 다. MediaPipe/LiteRT-LM 은 NPU 직접 선택을 지원하지 않는다(NPU 는 AICore/
> QNN 별도 경로). 아래 NPU 논의는 "QNN 을 별도로 태우는 고급 경로"로 읽고, 기본 배포는
> GPU 백엔드로 상정하라.

### 8.1 NPU와 각 변형의 적합성

Hexagon NPU(HTP)는 **상주 모델 + 고정 shape + INT8/INT4**에 최적화된 장치다.
사전 컴파일된 context binary와 소용량 TCM 타일링을 전제로 하므로, 우리 설계와
다음처럼 갈린다:

| 요소 | NPU 적합성 | 비고 |
|---|---|---|
| 변형 A (500MB 상주 코어/draft) | **적합** | INT4로 변환해 상주 실행, 저전력 |
| 변형 B (9GB 스트리밍 MoE) | 부적합 | 토큰별 가중치 스왑을 HTP가 싫어함 → CPU/GPU + mmap |
| 1.58bit 삼진 | 네이티브 미지원 | INT4/INT8로 디퀀트 실행. BitNet 이점은 디스크·RAM에만 잔존 |
| MoE 동적 라우팅 | 부분 부적합 | 제어 흐름은 CPU/GPU 폴백 잦음 |

**결론**: 계층이 하드웨어로 자연스럽게 분리된다 — **draft/상주 코어는 NPU(INT4),
대형 MoE 검증자는 CPU/GPU 스트리밍.** NPU는 "모델이 다 들어갈 때" 쓰는 장치이지
"안 들어가서 스왑할 때" 쓰는 장치가 아니다.

### 8.2 정식 배포 경로 (프로프라이어터리 .so 추출 금지)

- Google AI Edge Gallery APK에서 `libQnnHtp*.so` / HTP skel(`libQnnHtpV79Skel.so`
  등)을 추출해 재사용하지 않는다: (a) Qualcomm QNN SDK 라이선스 소지, (b) skel은
  DSP 아키텍처 버전에 묶여 이식성 없음, (c) Gallery의 `.task`/`.litertlm` 포맷은
  우리 삼진 가중치와 무관.
- 정식 경로: **Qualcomm QNN SDK / AI Hub**에서 델리게이트 확보 →
  **LiteRT(TFLite) QNN delegate** 또는 **MediaPipe LLM Inference API** 사용.
- 우리 모델 온디바이스화 파이프라인: `우리 체크포인트 → LiteRT 변환 →
  INT4 양자화 → QNN delegate 컴파일(context binary) → NPU 실행`. 남의 .so가
  아니라 우리 모델을 이 파이프라인에 태워야 한다.

### 8.3 리스크

- Hexagon LLM 지원은 특정 변환 모델에 한정적 — 커스텀 아키텍처(반복 블록,
  가변 깊이)는 델리게이트 미지원 op가 나올 수 있어 부분 CPU 폴백 대비 필요.
- context binary는 shape 고정 — 가변 컨텍스트/KV 캐시 처리에 별도 설계 필요.

## 9. 교사-학생 vocab 정렬: ULD vs 토크나이저 이식

Gemma 4(vocab 262,144)를 교사로 로짓 증류할 때 학생 LFM2.5(vocab 65,536)와의
불일치를 어떻게 다룰지의 결정.

### 9.1 두 선택지

| | ULD (cross-tokenizer) | 토크나이저 이식(transplant) |
|---|---|---|
| 방식 | 정렬-확률 L1 손실, vocab 무관 | 학생 vocab을 262k로 교체 후 순정 full-KL |
| 신호 강도 | 중~강 (정렬 근사) | 강 (순정 KL) |
| 크기 영향 | **없음** (65k 유지) | **+200M** 임베딩 → 350M→~550M |
| 추가 학습 | SFT 수준 | continued pretraining(수십억 토큰) |
| 온디바이스 목표 | 부합 | **충돌** (임베딩이 모델의 49%) |

측정치: LFM2.5-350M = vocab 65,536 / hidden 1,024 / tie_embedding=true.
임베딩 = 65,536×1,024 = 67M(전체 19%). 262k로 이식 시 262,144×1,024 = 268M(전체 49%).

### 9.2 결정

**기본 경로 = ULD** (`train_distill_logit.py`). 크기 페널티 0, 목표 부합.

토크나이저 이식은 "순정 full-KL이 반드시 필요"하고 크기·학습비용을 감수할 때만.
그 경우 크기 완화책을 병행한다:
- **인수분해 임베딩(ALBERT식)**: 262k×1024 를 262k×128 · 128×1024 로 분해 →
  임베딩 268M → ~34M 로 축소. 이식을 현실화하는 핵심 완화책.
- **임베딩 저정밀 양자화**: 임베딩/LM head 는 2bit 수준 양자화 내성이 크다 →
  배포 크기 타격 완화(파라미터 수는 그대로지만 GGUF 용량은 줄어듦).
- **FVT식 초기화**: 새 토큰 임베딩을 구 토크나이저 구성 조각들의 평균으로 초기화해
  continued pretraining 수렴을 가속.

### 9.3 권고

크기가 곧 제품인 이 프로젝트에서 이식은 순이득이 아니다(본체 지능 불변 + 크기 2배).
ULD로 로짓 신호를 얻고, 그래도 부족하면 이식 대신 **학생을 350M→700M/1.2B로 키우는
쪽**이 같은 크기 예산에서 더 나은 투자다(늘어난 파라미터가 본체 지능에 쓰이므로).

## 10. 온디바이스 학습(증류)이 안 되는 이유 — 정정판

정정: 폰에서 Gemma E4B 는 **NPU 가 아니라 GPU(Adreno, OpenCL/Vulkan)** 로 돈다.
MediaPipe/LiteRT-LM 의 Android 가속 경로는 GPU(OpenCL)이고, NPU 직접 선택은 지원되지
않는다(진짜 NPU 가속은 AICore/QNN 별도 경로). 따라서 §8 의 "NPU 배포" 프레이밍도
"모바일 GPU(OpenCL/Vulkan) 배포"로 읽어야 한다.

그리고 NPU 와 달리 **GPU 는 범용 연산이라 backward(gradient)를 원리적으로 수행할 수
있다.** 즉 "backward 가 불가능"해서 학습이 안 되는 것이 아니다. 진짜 병목은 소프트웨어다:

- **모바일 GPU 용 training 프레임워크가 없다.** ExecuTorch(Vulkan/OpenCL), LiteRT GPU,
  MediaPipe, llama.cpp Vulkan — 전부 **추론 전용**이다. autograd + 옵티마이저를 모바일
  GPU 에서 돌리는 스택이 존재하지 않는다.
- 그러므로 OpenCL/Vulkan 으로 폰에서 증류하려면 attention·conv(LFM2 하이브리드)·
  layernorm·softmax·cross-entropy·Adam 의 **backward 커널을 전부 직접 구현**해야 한다.
  이는 런타임 "개조"가 아니라 **훈련 프레임워크를 밑바닥부터 작성**하는 일이다.
- **처리량/발열**: Adreno GPU 는 수 TFLOPS + thermal throttling + LPDDR 공유라, 클라우드
  T4(~65 TFLOPS, ~$0.5/h)보다 20~100× 느리다. 클라우드 2시간 ≈ 폰 며칠(스로틀 포함).
- 참고: 모바일 GPU 백엔드가 지원하는 LoRA 는 **사전학습된 어댑터의 추론 적용**(MediaPipe/
  LiteRT, attention 한정)이지 on-device 학습이 아니다.

**실제 해결책(GPU 소유 불필요)**: 증류는 1회성이다. Google Colab 무료 GPU(T4)나
클라우드 GPU 를 몇 시간(~$1~2) 빌려 한 번 학습 → ~200MB(또는 LoRA 어댑터) 산출 →
이후 폰 GPU(OpenCL/Vulkan)에서만 돈다. 폰의 LoRA 추론 적용과도 맞물린다:
**클라우드에서 LoRA 학습 → 폰 GPU 백엔드에서 적용.** 1회 $1 단계를 없애려 훈련
프레임워크를 새로 만드는 것은 잘못된 트레이드다.
